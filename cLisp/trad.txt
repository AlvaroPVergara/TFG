(setq a 0)
(setq b 0)
(defun fun1 () (
(setq a (if (and (/= 3 0) 1) (
1)
)
)
(defun fun2 () (
(setq b (> 3 1))
)
(defun main () (
(setq c 0)
(print "Hola mundo")
(loop while  (<= c 6)  do 
(setq c (+ c 1))
)
(return-from main 3)
(let (setq bucle 0)
)
(loop while  (< bucle 5)  do 
(setq c (+ c 2))
(setq bucle (+ bucle 1))
)
(if  (> c 5)  (
(fun2 1 3 45 43)
(return-from main 3)
)
(
(return-from main 4)
)
c
)
