(setq a 0)
(setq b 0)
(defun fun1 () (
(setq a (if (and (/= 3 0) 1) (
(/= 3 0))
)
)
(defun fun2 () (
(setq b (> 3 1))
)
(defun main () (
(setq c 0)
(print "Hola mundo")
(setq c (+ 5 1))
(if  (> c 5)  (
(> c 5))
(
(return-from NUMERO )
)
c
)
