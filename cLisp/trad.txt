(setq a 0)
(setq b 0)
(defun fun1 () (
(setq a (if ((and (/= 3 0) 1)) (
1
0
)
)
)
(defun fun2 () (
(setq b (> 3 1))
)
(defun main () (
(setq c 0)
(print "Hola mundo")
(return-from main 3)
(fun2 1 3 45)
(setq c (+ 5 1))
c
)
